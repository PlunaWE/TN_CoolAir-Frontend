# Sommerfrische Frontend

Vite + React + TypeScript frontend matching the new landing-page layout.

## Run
```powershell
npm install
copy .env.example .env
npm run dev
```

## Notes
- Replace the image placeholders with your real `.png` and `.webp` files later.
- The product selection happens on the landing page.
- `Weiter zur Kasse` adds the selected offer to cart and navigates to `/checkout`.
- Checkout and payment pages stay compatible with the backend contract.


Updated landing page with carousel, placeholder logos, and clickable Sommerfrische logo.


Updated home page restores hero section and keeps Sommerfrische logo routed to home.
